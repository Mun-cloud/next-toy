model User {
  id           Int            @id @default(autoincrement())
  email        String         @unique
  name         String?
  cocoments    CoComment[]
  comments     Comment[]
  posts        Post[]
  newsBookmark NewsBookmark[]
}
